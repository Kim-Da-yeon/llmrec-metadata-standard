# 국제 메타데이터 표준을 통합한 LLM 기반 그래프 증강 추천시스템

**Standard-Aware LLM Graph Augmentation for Recommendation:
A Quantitative Trade-off Analysis between Recommendation Performance and Metadata Standard Compliance**

표준학개론 (INTRODUCTION TO STANDARDS) 기말 텀페이퍼
2026학년도 1학기

---

## 초록 (Abstract)

본 연구는 대형 언어모델(LLM)을 활용한 추천시스템 그래프 증강 기법인 LLMRec(WSDM '24)에 국제 메타데이터 표준 분류체계 — schema.org/Movie, ISO 3166-1 alpha-2(국가), BCP 47(언어), ISO 8601(날짜) — 를 통합하고, 추천 성능과 표준 적합성 간의 트레이드오프를 정량적으로 분석한다. LLMRec이 공개한 17,366개 Netflix 영화 메타데이터를 직접 분석한 결과, 동일 국가가 'USA'(8,091건)/'United States'(1,995건)/'America' 등으로 1,166개의 표면 토큰으로 분산되어 있고, 'country' 같은 LLM 환각이 835건 등장하는 등 표준 준수가 결여된 상태임을 정량적으로 확인하였다. 본 연구는 (i) schema.org/Movie JSON-LD 프롬프트 템플릿 *설계*, (ii) pycountry 기반 후처리 정규화 모듈 *구현*, (iii) 동일 표준 코드를 공유하는 아이템 임베딩의 centroid 통합으로 표준 효과를 시뮬레이션, (iv) Standard Compliance Rate (SCR)·Interoperability Score (IS) 두 신규 지표 제안의 네 가지 기여를 통합한다. 실험 결과 표준 적용 후 SCR은 0.484→0.914, IS는 0.000→0.803으로 크게 개선되었으며, **추천 성능 또한 R@20 0.08130→0.08347 (+2.67%), N@20 0.03350→0.03497 (+4.39%)로 동시에 개선되어 LLMRec 논문의 SoTA 보고치(R@20=0.0829)를 상회**하였다. 7개 추천 지표 중 6개에서 개선이 관찰되었으며, NDCG 평균 개선폭(+3.8%)이 Recall 평균 개선폭(+1.5%)보다 커서 표준화가 *상위 순위 정확성*에 더 크게 기여함을 시사한다. 이는 표준 준수가 단기 성능 손실을 수반한다는 표준학의 통상적 가정에 대한 반례 사례 연구가 된다.

**Keywords**: 표준학, schema.org, ISO 3166, BCP 47, LLM, 추천시스템, 메타데이터 상호운용성, 데이터 증강

---

## 1. 서론

### 1.1 연구 배경

추천시스템에서 데이터 희소성(data sparsity)과 cold-start 문제는 신규 사용자·아이템에 대한 상호작용 데이터가 부족할 때 발생하는 본질적 난제이다. 최근 Wei et al.(2024)이 제안한 **LLMRec**은 LLM에게 사용자의 상호작용 이력을 입력하여 (i) 잠재적 user-item 상호작용 edge, (ii) item 속성(director, country, language 등), (iii) user profile(country, liked genre 등)을 자연어로 생성·증강하여 그래프 신경망(GNN) 추천기의 학습 신호를 풍부화하는 방법을 제시하였다.

LLMRec은 Netflix 데이터에서 Recall@20 0.0829를 달성하여 LATTICE, MMSSL, MICRO 등 기존 SoTA 대비 11–14% 성능 향상을 보였다. 그러나 LLMRec이 LLM에게 **자유 텍스트** 형태로 메타데이터를 생성하도록 지시하는 설계는 표준학적 관점에서 다음과 같은 근본적 한계를 노정한다.

### 1.2 문제 정의

본 연구는 LLMRec의 공개 augmented data(`augmented_attribute_dict`, n=17,366 Netflix items)를 직접 분석하여 다음 세 가지 표준 미준수 패턴을 정량적으로 확인하였다 (Table 1).

**Table 1. LLM augmented data의 표준 미준수 패턴.**

| 패턴 | 정량 증거 (Netflix LLM augmented data, n=17,366) |
|---|---|
| **국가 표면 분산** | 동일 국가가 `USA`(8,091), `United States`(1,995), `America`, `U.S.A`, `U.S.` 등으로 표기. 고유 국가 토큰 1,166개 (ISO 3166-1 alpha-2 표준은 ~250개) |
| **언어 표면 분산** | `English`(12,444), `Mandarin`/`Chinese`/`Cantonese` 분리 등. 고유 언어 토큰 326개 (BCP 47 표준은 ~180개) |
| **컬럼 시프트 환각** | 빈 director field가 발생하면 후속 필드가 밀려 country 자리에 'country' placeholder가 등장 (835건). 언어 자리에 'USA' 국가명이 등장 (492건) |

이러한 표준 미준수는 임베딩 공간에서 의미적으로 동일한 속성이 분산되어 cold-start 일반화 성능을 저해하고, 외부 시스템과의 메타데이터 상호운용성도 확보하지 못한다.

#### 1.2.1 실데이터 관찰 (Netflix LLM augmented dataset)

LLMRec의 공개 데이터(`augmented_attribute_dict`)에서 영화 5건을 무작위로 추출하면 다음과 같다.

```text
영화 0번: director='Jim Jarmusch', country='USA',          language='English'
영화 4번: director='Brian Smith',  country='United States', language='English'
영화 9번: director='Bong Joon-ho', country='South Korea',   language='Korean'
영화 X:  director='?',             country='country',       language='USA'  ← LLM 환각
영화 Y:  director='—',             country=' ',             language='—'     ← 결측 표지자
```

이로부터 다음 세 가지 표준 미준수 패턴이 확인된다.

1. **표면 분산**: 동일 의미 토큰이 여러 표기로 표현됨. 컴퓨터 입장에서 `'USA'`와 `'United States'`는 완전히 다른 단어이므로, 같은 미국 영화임에도 임베딩 공간에서 분리된 점으로 학습된다.
2. **LLM 환각**: 출력 양식이 강제되지 않은 자유 텍스트 프롬프트의 부산물로, GPT가 placeholder 단어(`'country'`, `'language'`)를 실제 값처럼 출력하는 사례가 발생한다 (835건).
3. **컬럼 시프트 오류**: GPT가 `director::country::language` 구분자 출력 시 director가 비어 있으면 후속 필드가 한 칸씩 밀려, 언어 자리에 국가명(`'USA'`)이 등장하는 파싱 오류가 발생한다 (492건).

이러한 패턴은 단순한 데이터 품질 문제가 아니라, **"LLM이 자유 텍스트로 메타데이터를 생성할 때 필연적으로 발생하는 구조적 문제"** 이며, 본 연구의 출발점이 된다.

### 1.3 연구 질문

본 연구는 다음 세 가지 연구 질문에 답한다.

- **RQ1** (Standardization → Performance). LLM 기반 데이터 증강 결과에 국제 메타데이터 표준(schema.org/Movie, ISO 639/3166)을 적용하면 추천 성능(Recall@K, NDCG@K)에 어떤 영향을 미치는가?
- **RQ2** (Where it matters). 표준 적용의 효과는 시나리오별로 어떻게 달라지는가? 특히 long-tail 아이템 등에서 어떻게 발현되는가?
- **RQ3** (Quantification). 추천 성능과 표준 적합성·상호운용성 간의 트레이드오프를 어떻게 정량화·평가할 수 있는가?

### 1.4 기여

본 연구의 기여는 네 가지이다.
1. **schema.org/Movie JSON-LD 프롬프트 템플릿 설계** — LLM 출력 시점에 표준이 개입하는 *standard-compliant data generation* 청사진을 P_I, P_U, P_UI 세 단계 모두에 대해 제시.
2. **후처리 정규화 모듈 구현** — pycountry + 별칭 사전 + 컬럼 시프트 거부 휴리스틱을 결합한 ~150줄 모듈로, raw LLM 출력의 82%–86%를 ISO 표준 코드로 자동 매핑.
3. **Centroid 기반 표준 효과 시뮬레이션** — 추가 LLM 비용 없이 표준 통합의 임베딩 수준 효과를 직접적으로 모델링하는 방법론 제안.
4. **Standard Compliance Rate (SCR) 및 Interoperability Score (IS)** — LLM 시대 추천시스템 메타데이터 품질을 평가하는 두 정량 지표를 제안하고, 본 연구 데이터에서 측정.

---

## 2. 관련 연구

### 2.1 LLM 기반 추천시스템 데이터 증강

LLM의 광범위한 세계 지식을 추천시스템의 보조 신호로 활용하는 시도는 활발히 이루어져 왔다. TALLRec(Bao et al., 2023), GPT4Rec(Li et al., 2023) 등은 LLM을 직접 추천 모델로 사용하나, **LLMRec(Wei et al., 2024)** 은 LLM을 *증강 도구*로만 사용하고 추천은 기존 GNN-CF 모델(LightGCN backbone)이 수행한다는 점에서 차별화된다. 후속 재현성 연구(Malitesta et al., 2025, RecSys '25)는 LLMRec의 결과가 robust함을 검증하였다.

### 2.2 메타데이터 표준화와 상호운용성

Schema.org는 Google, Microsoft, Yahoo, Yandex가 공동 운영하는 의미론적 마크업 어휘로, Movie, Person, Organization 등 750+ 타입을 정의한다. ISO 3166-1 alpha-2는 국가 코드를, BCP 47(IETF RFC 5646)은 언어 태그를 표준화한다. 그러나 LLM 출력 메타데이터를 이러한 표준에 정합시키는 연구는 본 연구가 알기로 거의 없다 — 기존 표준화 연구는 사후 데이터 정리(data cleaning)에 한정되어 있었다.

### 2.3 본 연구의 위치

본 연구는 LLMRec의 추천 파이프라인에 표준 적합성을 명시적으로 통합한 첫 시도이며, 표준 준수와 추천 성능 간의 트레이드오프를 정량 측정한 점에서 **LLM × 추천시스템 × 표준학**의 교차 영역에 새로운 사례 연구를 제공한다.

---

## 3. 방법론

### 3.1 베이스라인 (Vanilla LLMRec)

LLMRec 공식 구현체(github.com/HKUDS/LLMRec)와 공개 augmented Netflix dataset을 그대로 사용한다. 추천 모델은 LightGCN backbone에 LLM-augmented user/item embedding을 보조 신호로 결합하는 구조이며, 손실 함수는 BPR + feature regularization + augmented BPR + multimodal BPR + (optional) attribute reconstruction의 다축 결합이다. Hyperparameter는 논문 default를 사용하였다 (`lr=1e-4`, `batch=1024`, `embed_dim=64`, `prune_loss_drop_rate=0.71`, `aug_sample_rate=0.1`).

### 3.2 Standard-Aware 통합 — 2단계 분리

본 연구는 표준 통합을 두 단계로 분리한다.

#### 3.2.1 Stage A — Standard-Aware Prompting *설계* (정성)

LLMRec의 기존 자유 텍스트 프롬프트
```
Provide the inquired information of the given movie.
[332] Heart and Souls (1993), Comedy|Fantasy
The inquired information is: director, country, language.
And please output them in form of: director, country, language
```
는 다음과 같은 표준 미준수 위험을 내포한다 — (i) 출력 어휘 미통제, (ii) 컬럼 시프트 시 후속 필드 오염, (iii) schema.org 필수 필드 결락.

이를 다음의 schema.org/Movie JSON-LD 프롬프트 `P_I*`로 재설계한다.
```text
[System]
You are a metadata extraction assistant strictly conforming to
schema.org/Movie. Output one JSON-LD object per movie with fields
encoded as machine-readable codes:
  - countryOfOrigin: ISO 3166-1 alpha-2
  - inLanguage: BCP 47 short tag
  - genre: schema.org genre vocabulary or EIDR genre code
  - contentRating: MPAA standard {G,PG,PG-13,R,NC-17,NR}
  - datePublished: ISO 8601 year (YYYY)
  - duration: ISO 8601 duration
  - director: schema.org Person.name
If a field cannot be determined, output null. Do NOT invent values.

[User]
{ "@context": "https://schema.org", "@type": "Movie",
  "identifier": "[332]", "name": "Heart and Souls",
  "datePublished": 1993 }
Complete the missing schema.org/Movie fields. Return one JSON-LD object only.
```

본 텀페이퍼에서는 OpenAI API 비용 회피와 일정 단축을 위해 **LLM 재호출은 수행하지 않는다**. P_I*, P_U*, P_UI* 세 템플릿 전체는 `prompt_templates.md`에 기술하였으며, 졸업논문 단계에서 sLLM(Llama-3-8B) 파인튜닝으로 prompt-time 통합을 실증할 예정이다.

#### 3.2.2 Stage B — 후처리 정규화 모듈 (정량 실험의 핵심)

LLMRec이 생성한 자유 텍스트 메타데이터를 ISO 표준 코드로 매핑하는 정규화 모듈을 구현한다 (`normalization.py`). 핵심 컴포넌트는:

- **`normalize_country(raw)`**: pycountry 직접 lookup → 별칭 사전(`USA`/`United States`→`US`; `UK`/`United Kingdom`→`GB`; `Soviet Union`→`RU`; `Czechia`→`CZ` 등) → 멀티 국가 분리(예: `"USA, UK"` → `US`) 의 fallback 체인. 결과 (`canonical_code`, `was_compliant`) 튜플 반환.
- **`normalize_language(raw)`**: BCP 47 alpha-2 lookup → 별칭 사전 → 컬럼 시프트 거부(`country_aliases ∪ ISO_3166`에 속하면 invalid). `'USA'`가 언어 자리에 등장 시 `None`으로 반환되어 다운스트림 신호 오염 차단.
- **`normalize_year(raw)`**: 1880–2030 범위 정수 검증 + 정규식 기반 추출(소수점 표기 `1993.0` 처리).
- **`INVALID_TOKENS`**: `{"", "country", "language", "n/a", "unknown", ...}` 등 LLM 환각 placeholder 명시적 거부.

#### 3.2.3 Centroid 기반 임베딩 갱신

표준 적합성의 임베딩 공간 효과를 추가 LLM 호출 없이 시뮬레이션하기 위해, 다음 절차로 표준 적용 임베딩을 생성한다 (`build_standardized_data.py`).

1. 각 아이템 i에 대해 정규화된 country code C_i, language tag L_i 산출.
2. 같은 코드를 공유하는 아이템 그룹 G(c) = {i : C_i = c} 정의.
3. 각 그룹의 기존 ada-002 (1536-dim) 임베딩의 centroid e_c = (1/|G(c)|) Σ_{i∈G(c)} e_i^country 계산.
4. 그룹 내 모든 아이템에 e_c 할당 → 표면 분산이 제거된 임베딩 공간.
5. 정규화 실패한 아이템(C_i = None)은 원본 임베딩 유지.
6. director, title, year 임베딩은 변경하지 않음 (표준화 효과를 *country/language 한정*으로 격리).

본 방법은 prompt-time 표준 통합의 *상한선 근사*로 해석할 수 있다 — LLM이 표준 코드만 출력하도록 강제했을 때 도달할 수 있는 임베딩 수준 효과를 직접적으로 모사한다.

### 3.3 평가 프레임워크

본 연구는 단일 성능 지표가 아닌 **3축 평가 프레임워크**를 도입한다.

#### 3.3.1 추천 성능

기존 LLMRec 평가 프로토콜(all-ranking)을 그대로 따른다.
- Recall@K, NDCG@K (K ∈ {10, 20, 50}), Precision@20.
- Test set: Netflix LLMRec split (n_test_users = 5,000).
- Metric은 best epoch 기준 (early stopping patience=7).

#### 3.3.2 Standard Compliance Rate (SCR) — *제안 지표 1*

특정 필드 f에 대해
$$\text{SCR}_f = \frac{|\{i : \text{raw}_f(i) \text{ is already in canonical standard form}\}|}{N}$$

전체 SCR은 필드 평균. 본 연구에서는 director, country, language, year (= datePublished) 4개 필드에 대해 측정.

#### 3.3.3 Interoperability Score (IS) — *제안 지표 2*

$$\text{IS} = \frac{|\{i : \text{모든 4개 schema.org/Movie 필수 필드가 표준 코드로 resolvable한 i}\}|}{N}$$

이 지표는 한 아이템이 외부 시스템으로 직접 export 가능한지를 binary로 측정한다.

---

## 4. 실험 및 결과

### 4.1 실험 환경

- 하드웨어: NVIDIA RTX PRO 6000 Blackwell (98GB VRAM)
- 소프트웨어: PyTorch 2.11 + CUDA 13.0, Python 3.12
- 데이터: Netflix Prize subset (LLMRec 공개), n_users=13,187, n_items=17,366, n_interactions=70,778, sparsity=0.00031
- 학습 시간: 1 epoch ≈ 3.5초 (RTX PRO 6000), 전체 수렴 ≈ 30초 (early stop)

### 4.2 베이스라인 재현 (RQ0)

본 연구의 분석 신뢰성을 확보하기 위해, 먼저 LLMRec 논문 Table 2의 Netflix 결과를 본 환경에서 재현하였다. 논문과 동일한 코드, 데이터, 하이퍼파라미터를 사용한 결과는 표 2와 같다.

**Table 2. LLMRec 논문 보고치 vs 본 연구 재현 (Netflix, all-ranking, best epoch).**

| 지표 | LLMRec 논문 (Wei et al., 2024) | 본 연구 재현 (Vanilla) | 일치율 |
|---|---|---|---|
| R@10 | 0.0531 | 0.05149 | 96.97% |
| **R@20** | **0.0829** | **0.08130** | **98.07%** |
| R@50 | 0.1382 | 0.13713 | 99.23% |
| N@10 | 0.0272 | 0.02596 | 95.44% |
| **N@20** | **0.0347** | **0.03350** | **96.54%** |
| N@50 | 0.0456 | 0.04454 | 97.68% |
| P@20 | 0.0041 | 0.00407 | 99.27% |

7개 지표 모두 95–99% 일치하여 본 베이스라인이 논문 결과를 충실히 재현하였음이 확인된다. 1–2 epoch 만에 수렴 후 점진 하락하는 양상은 LLM-augmented 임베딩이 학습 시작 시점에 이미 강한 신호를 제공하기 때문으로 해석된다 (early stopping patience=7).

### 4.3 RQ1: Standard-Aware vs Vanilla — 추천 성능

표 3은 본 연구의 핵심 비교 결과이다 — 동일한 코드·데이터·하이퍼파라미터에서 표준 적용 여부만 변경.

**Table 3. Standard-Aware의 7개 지표 개선 (Netflix, all-ranking, best epoch).**

| 지표 | Vanilla (재현) | **Standard-Aware** | Δ |
|---|---|---|---|
| R@10 | 0.05149 | **0.05257** | **+2.10%** |
| **R@20** | **0.08130** | **0.08347** | **+2.67%** |
| R@50 | 0.13713 | 0.13659 | -0.39% |
| N@10 | 0.02596 | **0.02725** | **+4.97%** |
| **N@20** | **0.03350** | **0.03497** | **+4.39%** |
| N@50 | 0.04454 | **0.04543** | **+2.00%** |
| P@20 | 0.00407 | **0.00417** | **+2.46%** |

**관찰**:
1. **7개 지표 중 6개에서 개선**. 유일하게 R@50이 -0.4% 미세 하락 (측정 노이즈 수준).
2. **NDCG 개선폭 (평균 +3.8%) > Recall 개선폭 (평균 +1.5%)** — 표준화가 단순 hit보다 *상위 순위 정확성*에 더 크게 기여. 같은 표준 코드를 공유하는 아이템들이 임베딩 공간에서 응축되어, top-K 내부 순서가 더 잘 정렬된다는 의미.
3. 프로포절 v1의 "성능 동등 또는 소폭 하락" 예상에 대한 **반증**.

### 4.4 RQ1 추가 맥락 — LLMRec 논문 Table 2 베이스라인들과의 비교

본 연구의 결과가 어느 위치에 있는지 보이기 위해, 논문 Table 2의 다른 모델들과 직접 비교한다 (모두 R@20).

**Table 4. LLMRec 논문 Table 2 + 본 연구 (Netflix R@20).**

| 모델 | R@20 | 출처 |
|---|---|---|
| MF-BPR | 0.0542 | 논문 |
| LightGCN | 0.0701 | 논문 |
| MMGCN | 0.0699 | 논문 |
| GRCN | 0.0706 | 논문 |
| LATTICE | 0.0737 | 논문 |
| MICRO | 0.0764 | 논문 |
| MMSSL | 0.0743 | 논문 |
| LLMRec (논문) | 0.0829 | 논문 |
| **LLMRec (본 연구 재현)** | **0.0813** | 본 연구 |
| **Standard-Aware LLMRec** | **0.0835** | **본 연구** |

본 연구의 Standard-Aware는 LLMRec의 논문 보고치 0.0829를 0.6% 미세하게 상회하여, 동일 데이터·동일 backbone에서 표준 통합만으로 SoTA 갱신이 가능함을 보인다.

### 4.5 RQ3: 표준 적합성 정량화

**Table 5. 표준 적합성 측정 (n=17,366).**

| 필드 | Raw SCR | Post SCR | Uplift | 고유 토큰 (Raw → Norm) | 압축비 |
|---|---|---|---|---|---|
| director | 0.936 | 0.936 | +0.000 | 8,759 → 8,755 | 1.0× |
| country | **0.000** | **0.858** | **+0.858** | 1,166 → 57 | **20.5×** |
| language | **0.000** | **0.866** | **+0.866** | 326 → 52 | **6.3×** |
| year (datePublished) | 0.999 | 0.999 | +0.000 | 95 → 94 | 1.0× |
| **Overall SCR** | **0.484** | **0.914** | **+0.430** | — | — |
| **Interoperability Score** | **0.000** | **0.803** | **+0.803** | — | — |

**해석**:
- LLMRec의 country, language 필드는 **단 한 건도 ISO 표준 표기로 출력되지 않았다** (Raw SCR = 0). 즉, LLMRec의 자유 텍스트 프롬프트는 표준 어휘를 *전혀* 활용하지 못한다.
- 후처리 정규화 후 country는 1,166개 고유 토큰을 57개 ISO 코드로 압축 (20.5×), language는 326개를 52개 BCP 47 태그로 압축 (6.3×).
- **17,366개 아이템 중 80.3%**가 schema.org/Movie 4개 핵심 필드를 모두 표준 코드로 산출 가능한 상태로 전환되었다 (IS = 0.803).

표 6은 country 필드의 정규화 전후 상위 8개 토큰의 변화를 보여준다.

**Table 6. country 필드 정규화 전후 상위 8개 토큰 (count).**

| Rank | Raw | Post (ISO 3166) |
|---|---|---|
| 1 | `USA` (8,091) | `US` (10,086) |
| 2 | `United States` (1,995) | `GB` (1,356) |
| 3 | `UK` (1,030) | `JP` (705) |
| 4 | `country` (835, 환각!) | `FR` (364) |
| 5 | `Japan` (705) | `IN` (313) |
| 6 | `France` (364) | `CA` (290) |
| 7 | `United Kingdom` (326) | `HK` (256) |
| 8 | `India` (313) | `IT` (167) |

`'USA'` + `'United States'` + `'America'` → `'US'` (10,086건 통합), `'UK'` + `'United Kingdom'` + `'England'` → `'GB'` (1,356건). LLM 환각인 `'country'` placeholder는 invalid 처리되어 다운스트림 신호 오염을 차단한다.

### 4.6 RQ2: Long-tail 분석은 본 데이터셋에서 측정 불가

프로포절은 RQ2에서 표준화의 long-tail / cold-item 효과 분석을 제안하였다. 그러나 LLMRec 저자들이 공개한 Netflix subset의 데이터 특성상 본 평가는 원천적으로 불가능함이 확인되었다.

- 학습 데이터 70,778건 중 17,366개 아이템 분포를 분석한 결과, **freq=0 (학습 인터랙션 0회) 아이템이 전체의 71.9% (12,481편)** 차지.
- LLMRec의 backbone인 LightGCN은 neighbor aggregation 기반이므로, freq=0 아이템은 collaborative signal이 0이 되어 어떤 모델로도 추천되지 않음 (Cold Recall@20 = 0.000, Vanilla / Std-Aware 양쪽 모두).
- LLMRec 논문 자체도 cold/warm 분리 평가를 보고하지 않으며 (Table 2는 전체 평균 R@K만), 이는 **공개 데이터셋의 한계**로 본 텀페이퍼 범위를 벗어난다.

이 분석은 졸업논문에서 (i) 더 dense한 ML-10M split, (ii) attribute-aware backbone(VBPR/MMGCN/MICRO)으로 확장하여 검증한다. 본 텀페이퍼에서는 이 한계를 정직하게 보고하고, 전체 평균 지표(§4.3 Table 3)에서 관찰된 표준화 효과로부터 long-tail 가설을 *간접 추론*하는 데 그친다.

### 4.7 트레이드오프 종합

**Table 7. 4축 종합 비교.**

| 축 | Vanilla | Std-Aware | 효과 |
|---|---|---|---|
| 추천 성능 (R@20) | 0.0813 | **0.0835** | ↑ +2.67% |
| 추천 순위 품질 (N@20) | 0.0335 | **0.0350** | ↑ +4.39% |
| 표준 적합성 (SCR) | 0.484 | **0.914** | ↑ +43%p |
| 상호운용성 (IS) | 0.000 | **0.803** | ↑ +80%p |
| 데이터 분산 (고유 country) | 1,166 | **57** | ↓ 20.5× |

본 데이터셋·모델 조합에서 표준 준수와 추천 성능 간에는 **유의한 트레이드오프가 관찰되지 않으며**, 두 축이 동시에 개선되는 *positive-sum* 효과가 나타났다.

---

## 5. 토의 (Discussion) — 표준학적 해석

### 5.1 왜 표준화가 추천 성능을 개선시켰는가?

본 결과는 표준화의 두 가지 효과의 합으로 해석된다.

**(a) 분산 억제 효과 (Variance Reduction)**: 의미적으로 동일한 토큰('USA', 'United States', 'America')이 ada-002 임베딩 공간에서 1,166개의 분산된 점으로 표현되던 상태에서, 57개의 centroid로 응축되며 group-level signal-to-noise ratio가 상승. GNN의 message passing이 이 응축된 attribute signal을 user embedding으로 전파할 때, 동일 의미 토큰들로부터의 inconsistent 신호가 사라져 user representation이 더 일관적으로 수렴한다.

**(b) 환각 차단 효과 (Hallucination Filtering)**: LLM 출력의 'country' placeholder(835건), 'USA' as language(492건) 같은 명백한 오류가 invalid 처리되어 다운스트림에서 결측치로 명시적 처리되거나 centroid 계산에서 제외된다. 즉, **표준화가 implicit data validation 역할을 수행**한 것이다.

이 두 효과의 결합이 표준 통합의 implicit regularization 효과로 작용한 것으로 해석된다.

### 5.2 표준학적 함의

표준학에서 표준 준수는 통상적으로 "단기 비용(준수 비용, 표현력 손실) vs 장기 이익(상호운용성, 생태계 호환)"의 트레이드오프 모델로 인식되어 왔다. 그러나 본 연구는 LLM 기반 데이터 증강 시나리오에서 **표준 준수가 단기 성능 측면에서도 손해를 보지 않을 수 있음**을 보여준다. 이는 표준이 단순한 inter-system 호환성 도구가 아니라, **데이터 품질 증진 도구로 재정의 가능함**을 시사한다.

이 관찰은 다음과 같이 일반화될 수 있다.
> *데이터 생성 단계에 표준이 개입하면, 표준 어휘는 자연어 출력의 entropy를 제어하는 정규화 항(regularizer)으로 작용한다. 그 결과 다운스트림 ML 시스템이 학습할 distribution이 cleaner해지며, 성능과 표준 준수가 양립한다.*

이는 표준학의 새로운 연구 축 — **"AI-readability"** 차원에서 표준의 가치를 평가하는 — 의 성립 가능성을 시사한다.

### 5.3 본 연구의 위치와 한계

본 연구는 표준 적용의 *상한선*을 centroid 방법론으로 시뮬레이션하였다. 실제 prompt-time 표준 통합(LLM이 직접 ISO 코드를 출력)이 동일한 성능 향상을 달성하는지는 졸업논문 단계에서 sLLM 파인튜닝으로 검증해야 한다. 본 결과가 centroid 방법론의 artifact일 가능성도 배제할 수 없다.

또한 schema.org/Movie 50+ 필드 중 본 연구는 4개 핵심 필드(director, countryOfOrigin, inLanguage, datePublished)만 검증하였다. Netflix LLM augmented data에 contentRating, duration, genre 데이터가 결락되어 있기 때문이며, 이는 데이터셋 가용성의 한계이다. MovieLens-10M 확장 시 genre 필드가 추가 가능하다.

마지막으로 long-tail 분석에서 cold-item recall이 0으로 나온 것은 LightGCN backbone의 0-degree 노드 처리 한계에 기인하며, 표준화 자체의 한계가 아니다.

---

## 6. 결론 및 향후 연구

### 6.1 결론

본 연구는 LLMRec의 LLM 기반 데이터 증강 파이프라인에 schema.org/Movie, ISO 3166-1, BCP 47 표준을 통합한 Standard-Aware LLMRec를 제안하고 평가하였다. 17,366개 Netflix 영화에 대해 후처리 정규화 + centroid 임베딩 방법으로 표준 효과를 시뮬레이션한 결과, **(i)** Recall@20 0.0813→0.0835 (+2.7%), NDCG@20 +4.5% 개선, **(ii)** Standard Compliance Rate 0.484→0.914, Interoperability Score 0.000→0.803으로 크게 개선, **(iii)** country 토큰 1,166개를 57개 ISO 코드로 20.5× 압축이 동시에 달성됨을 확인하였다. 본 결과는 표준 준수와 추천 성능이 양립 가능함을 보이는 사례 연구로, 표준학에서 표준의 가치를 "AI-readability" 차원에서 재해석할 수 있는 가능성을 제시한다.

### 6.2 향후 연구 (졸업논문 연계)

본 텀페이퍼의 결과물은 석사 졸업논문 「멀티모달 증강을 통한 추천시스템 Cold Start 문제 해결」의 선행 연구로 다음과 같이 확장된다.
1. **Prompt-time 표준 통합 실증**: 본 텀페이퍼의 P_I*, P_U*, P_UI* 템플릿을 sLLM(Llama-3-8B)에 적용하여 실제 LLM 출력 수준에서 표준 효과 검증.
2. **다 backbone 확장**: VBPR, MMGCN, MICRO 등 attribute-aware backbone에서 표준화 효과의 모델 무관성 검증.
3. **Cross-domain 전이**: MovieLens-10M ↔ Netflix 표준 코드 기반 zero-shot 전이 실험.
4. **EIDR/MPAA 등 추가 표준 통합**: contentRating, duration, genre 필드까지 확장.

---

## 참고문헌

1. Wei, W., Ren, X., Tang, J., Wang, Q., Su, L., Cheng, S., Wang, J., Yin, D., & Huang, C. (2024). LLMRec: Large Language Models with Graph Augmentation for Recommendation. *Proc. WSDM '24*.
2. Malitesta, D., et al. (2025). How Powerful are LLMs to Support Multimodal Recommendation? A Reproducibility Study of LLMRec. *Proc. RecSys '25*.
3. Bao, K., Zhang, J., et al. (2023). TALLRec: An Effective and Efficient Tuning Framework to Align Large Language Model with Recommendation. *arXiv:2305.00447*.
4. He, X., Deng, K., Wang, X., Li, Y., Zhang, Y., & Wang, M. (2020). LightGCN: Simplifying and Powering Graph Convolution Network for Recommendation. *Proc. SIGIR '20*.
5. Schema.org. *Movie - Schema.org Type*. https://schema.org/Movie (accessed 2026-04-30).
6. EIDR. *Entertainment Identifier Registry — ISO 26324-based content identifier system*. https://www.eidr.org/ (accessed 2026-04-30).
7. ISO 3166-1 (2020). *Codes for the representation of names of countries and their subdivisions — Part 1: Country code*. International Organization for Standardization.
8. ISO 639-1 (2002). *Codes for the representation of names of languages — Part 1: Alpha-2 code*. International Organization for Standardization.
9. IETF (2009). *BCP 47 / RFC 5646: Tags for Identifying Languages*.
10. ISO 8601 (2019). *Date and time — Representations for information interchange*.

---

## 부록 A — 산출물 명세

| 파일 | 역할 | 위치 |
|---|---|---|
| `normalization.py` | ISO 3166 / BCP 47 정규화 모듈 | `LLREC_표준학/work/` |
| `compliance_metrics.py` | SCR / IS 측정 | 동일 |
| `build_standardized_data.py` | Centroid 표준 임베딩 생성 | 동일 |
| `cold_start_eval.py` | Long-tail Recall 평가 | 동일 |
| `prompt_templates.md` | schema.org/Movie JSON-LD 프롬프트 설계 (Stage A) | 동일 |
| `RESULTS_SUMMARY.md` | 실험 결과 요약표 | `LLREC_표준학/work/results/` |
| `compliance_summary.json` | SCR raw output | 동일 |
| `baseline_train.log` / `standard_aware_train.log` | 학습 로그 | 동일 |

전체 코드와 결과는 본 텀페이퍼 제출물의 일부로 제공된다.

## 부록 B — 재현 명령어

```bash
# 1. LLMRec clone & 데이터 다운로드
git clone https://github.com/HKUDS/LLMRec
gdown --folder "https://drive.google.com/drive/folders/1BGKm3nO4xzhyi_mpKJWcfxgi3sQ2j_Ec" -O LLMRec/data/

# 2. NumPy 2.0 호환성 패치 (utility/metrics.py: np.asfarray → np.asarray(..., dtype=float))

# 3. 표준 적용 데이터 생성
cd LLREC_표준학/work
python3 build_standardized_data.py
python3 compliance_metrics.py

# 4. Vanilla 베이스라인
cd ~/LLMRec
python3 main.py --dataset netflix_valid_item

# 5. Standard-Aware 실험
python3 main.py --dataset netflix_std
```
