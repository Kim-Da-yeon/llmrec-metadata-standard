# LLM 생성 추천 메타데이터의 표준 적합성 진단과 정규화
### — ISO 3166-1 / BCP-47 기반 메타데이터 품질 개선

> 대상 시스템: LLMRec (HKUDS) · 데이터: Netflix Prize subset (항목 17,366개)
> 표준: ISO 3166-1 alpha-2(국가), BCP 47(언어), ISO 8601(연도)

---

## 초록 (Abstract)

대규모 언어모델(LLM)을 활용한 추천 시스템은 LLM이 생성한 항목 메타데이터(국가·언어 등)를
입력으로 사용한다. 그러나 LLM은 자유 텍스트를 산출하므로, 같은 국가가 "USA", "United States",
"America"처럼 **비일관 형식**으로 표현되어 시스템 간 상호운용과 데이터 통합을 저해한다. 본 연구는
LLM이 생성한 Netflix 메타데이터의 **표준 적합성을 정량 진단**하고, ISO 3166-1 / BCP-47 정규화
레이어를 통해 이를 개선한다. 진단 결과 국가·언어 필드는 표준 코드 형식으로 산출되지 않아
**1,166개의 표면형이 난립**(정준 57종, **20.5× 중복**)하고 **724건의 형식 오류(컬럼시프트)**를
포함하였다. 정규화 레이어는 이를 정준 표준 코드로 통합하여 **상호운용 가능 항목을 80.3%로**
끌어올리는 동시에 LLM 생성 오류를 자동 검출하였다. 본 연구는 LLM 증강 추천 데이터에 대한
**표준 기반 품질 관리의 필요성**을 실증적으로 보인다.

**주제어:** 표준화, 메타데이터 품질, ISO 3166-1, BCP-47, 상호운용성, LLM 기반 추천

---

## 1. 서론

### 1.1 문제 정의
LLM 기반 추천 시스템(LLMRec 등)은 부족한 항목 메타데이터를 LLM으로 생성·증강하여 사용한다.
그러나 LLM은 통제 어휘(controlled vocabulary)가 아니라 **자유 텍스트**를 출력하므로, 동일한
대상이 서로 다른 표면형으로 산출된다. 이러한 비표준·비일관 메타데이터는
(i) 시스템 간 상호운용, (ii) 집계·검색·분석, (iii) 이종 데이터셋 통합을 직접적으로 저해한다.
본 연구는 이 문제를 **표준화 관점**에서 진단하고 개선한다.

### 1.2 연구 목표
LLM이 생성한 추천 메타데이터가 국제 표준에 얼마나 적합한지 정량 진단하고, 사후(post-hoc)
정규화 레이어를 통해 표준 적합성·상호운용성을 얼마나 개선할 수 있는지 보인다.

---

## 2. 방법

### 2.1 표준 정규화 레이어
LLM 자유 텍스트를 정준 표준 코드로 매핑하는 사후 정규화 모듈을 구현한다.

| 필드 | 목표 표준 | 처리 |
|---|---|---|
| 국가(country) | ISO 3166-1 alpha-2 | 별칭 사전 + `pycountry` 조회 + 다중국가 분해 |
| 언어(language) | BCP 47 (alpha-2/3) | 별칭 사전 + 컬럼시프트 오류 거부 |
| 연도(year) | ISO 8601 (datePublished) | 4자리 연도 정규화 |
| 감독(director) | (해당 표준 없음) | 공백 정규화만 |

각 매핑은 `(정준 코드, 원래 표준 형식 적합 여부)`를 반환하여 **적합성 측정**과 **오류 탐지**를
동시에 수행한다.

### 2.2 평가 지표
- **형식 적합률**: 원본 문자열이 이미 정준 표준 코드 형식과 일치하는 비율
- **압축비**: 고유 표면형 수 / 정준 코드 수 (표현 중복 정도)
- **상호운용성 점수(IS)**: 4개 필드가 모두 유효한 schema.org/Movie JSON-LD로 변환되는 항목 비율
- **오류 검출**: 정규화 불가·형식 위반(컬럼시프트 등) 항목 수

---

## 3. 결과

### 3.1 표준 형식 비일관성과 정규화 효과
LLM 생성 메타데이터는 값 자체는 유효하나 **표준 코드 *형식*이 아니다**(예: "USA"는 올바른
국가명이지만 ISO 코드 "US"는 아님). 즉 핵심 문제는 값의 오류가 아니라 **형식 비일관성**이다.

| 필드 | 형식 적합률† | 정규화 후 | 고유 표면형 → 정준 | 압축비 |
|---|---|---|---|---|
| **country** | 0.000 | 0.858 | **1,166 → 57** | **20.5×** |
| **language** | 0.000 | 0.866 | **326 → 52** | **6.3×** |
| director | 0.936 | 0.936 | (표준 없음) | — |
| year | 0.999 | 0.999 | — | — |
| **전체** | 0.484 | 0.914 | — | — |

† 국가·언어가 0인 것은 LLM이 자유 표면형("USA","United States" 등)을 출력하기 때문이며,
값의 오류가 아니라 형식 비일관을 의미한다 — 같은 국가가 **1,166개 표면형**으로 난립한다.

→ 정규화로 표면형이 정준 코드로 통합되어 **국가는 20.5배, 언어는 6.3배 압축**되었다.

### 3.2 상호운용성
**정규화 후 항목의 80.3%가 4개 필드 모두 유효한 schema.org/Movie JSON-LD로 변환 가능**하다
(원본은 국가·언어의 형식 비일관으로 거의 0). 즉 정규화 레이어가 데이터셋의 상호운용 가능 비율을
사실상 0에서 **80.3%**로 끌어올린다.

### 3.3 표준 기반 LLM 오류 탐지
표준 정규화는 LLM 출력의 **오류 검출기**로도 기능한다. 정규화 불가·형식 위반 항목을 정량화하면:

| 오류 유형 | 검출 건수 | 비율 |
|---|---|---|
| country 정규화 불가(비표준·이상 출력) | 2,471 | 14.2% |
| language 정규화 불가 | 2,335 | 13.4% |
| language 칸에 국가가 삽입된 **컬럼시프트 오류** | 724 | — |
| country 빈값/placeholder | 1,175 | — |

→ 표준화는 단순 형식 통일을 넘어 **LLM 생성 오류(필드 혼입·결측 등)를 자동 검출**한다.

---

## 4. 한계

1. **단일 데이터셋.** Netflix 메타데이터에 한정하였다. 도메인·언어 구성이 다른 데이터셋에서의
   일반화는 검증되지 않았다.
2. **표준 적용 가능 필드의 제약.** 국제 표준이 존재하는 국가·언어에 국한된다. 감독(인명)·제목은
   통제 어휘 표준이 없어 정규화 대상에서 제외된다.
3. **사후(post-hoc) 정규화.** 생성된 결과를 사후에 매핑하는 방식으로, 생성 시점에 표준을 강제하지
   않는다. 별칭 사전 기반이므로 사전에 없는 희귀 표기는 정규화에 실패할 수 있다(국가 14.2% 잔존).
4. **추천 성능과의 직접 연계 부재.** 본 연구는 데이터 품질·상호운용성 개선에 초점을 두며, 표준화가
   추천 정확도에 미치는 영향은 별도의 검증 과제이다.

---

## 5. 원인 및 시사점

### 5.1 비표준 출력의 원인
LLM은 **통제 어휘가 아니라 자연어 분포를 학습**하므로, 사람이 흔히 쓰는 표면형("USA",
"United States")을 우선 산출하며 ISO 코드 같은 정준 형식을 따르지 않는다. 또한 생성 시점에
**스키마·표준 제약이 부여되지 않아** 같은 대상이 다양한 표면형·필드 혼입 형태로 나타난다.
즉 비표준성은 LLM 생성 파이프라인의 구조적 특성이다.

### 5.2 시사점
- **표준화의 가치는 데이터 품질·상호운용성에 있다.** 같은 국가의 1,166개 표면형은 검색·필터링·
  집계·데이터셋 통합을 직접적으로 저해하며, 정규화는 이를 정준 코드로 통합해 해결한다.
- **LLM 증강 파이프라인에는 표준 기반 품질 검증 레이어가 필요하다.** 표준은 형식 통일뿐 아니라
  생성 오류(724건 컬럼시프트 등)를 탐지하는 게이트로 기능한다.

---

## 6. 향후 연구

1. **생성 시점 표준 강제.** 사후 정규화 대신 구조화 출력(JSON 스키마·열거형 제약 디코딩)으로
   생성 단계에서 표준 코드를 직접 산출하도록 하여 비일관·오류를 원천 차단.
2. **다도메인·다필드 표준 확장.** 장르(콘텐츠 분류), 콘텐츠 등급 등 추가 표준 어휘로 확장하고
   도메인 일반화를 검증.
3. **표준화의 다운스트림 효과 정밀 검증.** 메타데이터가 더 큰 비중을 차지하는 과제·데이터셋에서
   표준화가 추천·검색 품질에 미치는 영향을 측정.

---

## 부록. 재현 정보
- 정규화 모듈: `work/normalization.py`
- 진단 스크립트: `work/compliance_metrics.py`
- 진단 결과(JSON): `work/results/compliance_summary.json`
